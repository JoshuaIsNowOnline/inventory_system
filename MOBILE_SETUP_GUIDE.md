# 📱 手機應用安裝指南

## 🤖 Android 手機安裝

### 1️⃣ 生成 APK 檔案
```bash
# Windows 用戶
build_app.bat
# 選擇選項 1

# Mac/Linux 用戶  
chmod +x build_app.sh
./build_app.sh
# 選擇選項 1
```

### 2️⃣ 安裝到手機
1. 將 `build/app/outputs/flutter-apk/app-release.apk` 複製到手機
2. 在手機上開啟「設定」→「安全性」→開啟「未知來源」
3. 點擊 APK 檔案進行安裝
4. 安裝完成後即可使用

---

## 🍎 iPhone 手機安裝

由於 iOS 限制，建議使用網頁版：

### 1️⃣ 生成網頁版
```bash
# Windows 用戶
build_app.bat  
# 選擇選項 2

# Mac/Linux 用戶
./build_app.sh
# 選擇選項 2
```

### 2️⃣ 部署到網頁伺服器
1. 將 `build/web/` 資料夾上傳到網頁伺服器
2. 或使用本地伺服器：
   ```bash
   cd build/web
   python -m http.server 8080
   ```

### 3️⃣ iPhone 使用方式
1. 開啟 Safari 瀏覽器
2. 訪問網頁版網址 (如：http://192.168.1.100:8080)
3. 點擊「分享」按鈕 → 「加入主畫面」
4. 應用圖標將出現在桌面，使用體驗類似原生 App

---

## 🔧 配置 API 連線

### 修改 API 網址
編輯 `frontend/inventory_app/lib/services/api_service.dart`：

```dart
class ApiService {
  // 修改為您的後端服務器地址
  static const String baseUrl = 'http://YOUR_SERVER_IP:7000';
  
  // 範例：
  // static const String baseUrl = 'http://192.168.1.100:7000';  // 本地網路
  // static const String baseUrl = 'https://your-app.railway.app'; // 雲端部署
}
```

### 重新打包
修改後需要重新執行打包命令。

---

## 📋 網路設置檢查清單

### ✅ Android
- [x] 後端服務器運行中 (port 7000)
- [x] 手機與服务器在同一網路
- [x] 防火牆允許 7000 端口
- [x] API 網址配置正確

### ✅ iPhone  
- [x] 網頁服務器運行中
- [x] Safari 可訪問網頁版
- [x] 後端 API 可正常連接
- [x] HTTPS (如果需要)

---

## 🚀 一鍵部署腳本

### Windows 完整部署
```batch
# 1. 啟動後端
cd backend
python app.py

# 2. 打包 Android (另一個終端)
build_app.bat

# 3. 啟動網頁版 (另一個終端)  
cd frontend/inventory_app
flutter build web
cd build/web
python -m http.server 8080
```

### macOS/Linux 完整部署
```bash
# 1. 啟動後端
cd backend && python app.py &

# 2. 打包應用
./build_app.sh

# 3. 啟動網頁版
cd frontend/inventory_app && flutter build web && cd build/web && python -m http.server 8080
```

---

## 🆘 故障排除

### Android APK 無法安裝
- 確認已開啟「未知來源」安裝權限
- 檢查手機空間是否足夠  
- 嘗試重新下載 APK 檔案

### iPhone 無法連接
- 確認使用 Safari 瀏覽器
- 檢查網路連線狀態
- 清除 Safari 暫存資料

### API 連接失敗
- 確認後端服務器運行狀態：http://SERVER_IP:7000/docs
- 檢查防火牆設定
- 驗證 IP 地址和端口號正確性

### 應用閃退或白屏
- 重新安裝應用
- 檢查網路連接
- 查看伺服器日誌錯誤訊息